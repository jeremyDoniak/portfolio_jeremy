<form method="post" action="{{ path('propos_delete', {'id': propo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ propo.id) }}">
    <button class="btn">Delete</button>
</form>
