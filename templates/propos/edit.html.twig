{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit Propos{% endblock %}

{% block body %}
    <h1>Edit Propos</h1>

    {{ include('propos/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('propos_index') }}">back to list</a>

    {{ include('propos/_delete_form.html.twig') }}
{% endblock %}
